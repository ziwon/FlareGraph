{
  "name": "@flaregraph/plugin",
  "version": "0.1.0",
  "private": true,
  "description": "FlareGraph Obsidian plugin: instant index trigger + inbox consolidation + status",
  "main": "dist/main.js",
  "scripts": {
    "build": "node esbuild.config.mjs && cp manifest.json dist/",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "obsidian": "^1.8.7",
    "typescript": "^5.8.0",
    "vitest": "^3.2.0"
  }
}
